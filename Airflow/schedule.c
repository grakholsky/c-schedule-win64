/* includes

// GET: /schedule
void ScheduleGet (HttpResquest httpResquest, HttpResponse httpResponse)
{
	return AFView (char* viewName, AFParams* params, void* viewSchedule);
}

// POST: /schedule
void SchedulePost (HttpResquest httpResquest, HttpResponse httpResponse)
{
	return AFView (char* viewName, AFParams* params, void* viewSchedule);
}

*/